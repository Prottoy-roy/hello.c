#include<stdio.h>
#include<math.h>
//create a program to find the least common multiple(lcm) of two numbers. 
int main(){
  int a=24;
  int b=30;
  int gcd= (24/2) * (30/2);
  // a = pow(2,3) * pow(3,1);
  // b = pow(2,2) * pow(3,1) * pow(5,1);
  // int lcm = pow(2,3) * pow(3,1) * pow(5,1);
  return 0;
}